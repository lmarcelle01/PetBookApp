<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PetBookApp.Views.OnBoardingPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="PetBook" FontFamily="{StaticResource AppLogo}"
                   VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"/>
            <Label Text="This is the BoardingPage"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Button Text="Sign In" Command="{Binding GoToSignInPageCommand}" Style="{StaticResource ButtonStyle}"/>
            <Button Text="Sign Up" Command="{Binding GoToSignUpPageCommand}" Style="{StaticResource ButtonStyle}"
                    Margin="0, 20,0,80"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>